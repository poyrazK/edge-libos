# HANDOFF.md — regenerated, uncommitted running status

> Regenerated by the P3 final-bundle PR. Committed once at the
> close of the milestone (force-added past `.gitignore` because
> this is the canonical milestone-end snapshot, not the
> session-local handoff that `.gitignore` excludes). Subsequent
> session-local handoffs follow the existing `.gitignore` rule.

**edge-libos** version: **0.2.0** (P3 final-bundle).

## P3 final-bundle PR — DOF (Definition of Finalized)

Per the plan at `/Users/poyrazk/.claude/plans/lets-create-imp-plan-jazzy-wind.md`,
the P3 final-bundle PR lands **seven sub-deliverables** on
`worktree-p3-final-bundle`:

| # | Sub-deliverable | Status |
|---|---|---|
| 1 | ADR 0003 (live x86→ARM migration) + `docs/adr/README.md` entry | ✅ committed |
| 2 | `Kernel.memory_kind` (`Owned(Memory) \| Shared(SharedMemory)`) migration | ✅ committed |
| 3 | pthread_mutex two-fiber test (`MemoryKind::Shared` end-to-end) | ✅ committed |
| 4 | `wait4` parked-Waker path (any-pid + specific-pid) | ✅ committed |
| 5 | `fork(57)` v1 handler + C conformance + 4 Rust integration tests | ✅ committed |
| 6 | `edge-cli migrate` subcommand + 3 Rust smoke tests (1 `#[ignore]`'d subprocess) + Shared-memory roundtrip test | ✅ committed |
| 7 | HANDOFF.md regen + `Cargo.toml` 0.2.0 + README bump | ✅ this commit |

## Test totals (P3 final-bundle close)

| Source | Count |
|---|---|
| Rust unit (`#[cfg(test)]` in `src/**`) | 123 |
| Rust integration (`tests/*.rs`) | 197 |
| C conformance (`tests/conformance/*.c`) | 105 |
| **Grand total** | **425** |

All 4 CI gates must be green on push:
- `cargo clippy --profile ci --all-targets -- -D warnings`
- `cargo build --profile ci`
- `bash tests/conformance/runner.sh` (105/105 PASS)
- `bash scripts/reproduce_dod.sh` (8-step DoD sequence)

## Architectural decisions (P3 final-bundle)

### ADR 0001 — futex semantics
Accepted (P3 Tier-1). `u32` guest addresses only; `-EFAULT` via
`mem::guest_slice`; `parking_lot::Mutex` for state, `tokio::sync::Notify`
for wakes; lock never held across `.await`. WAKE does not fire
existing `epoll_wait` subscribers.

### ADR 0002 — snapshot wire format
Accepted (P3 Tier-2). `postcard` + serde. Sparse per-page layout;
`LeU*` / `LeI*` newtypes for endian-explicit integers. Format
version stays at 1 — additive end-of-struct fields don't bump
the version.

### ADR 0003 — live x86→ARM migration
Accepted (P3 final-bundle). `wasm32` modules are arch-agnostic;
the same `.wasm` runs on x86 and ARM hosts. `KernelSnapshot` is
byte-identical across hosts. v1 migration is freeze → copy
snapshot → serve; no online drain. `Module::serialize` is
host-arch-specific — each host re-compiles from the `.wasm`.
TCP connection migration is out of scope forever for v1.

### MemoryKind dispatch (sub-deliverable 2)
`Kernel.memory: Option<Memory>` → `Option<MemoryKind>` where
`MemoryKind = Owned(Memory) | Shared(SharedMemory)`. The Owned
variant goes through the existing `apply_snapshot_to_memory`;
the Shared variant uses a private
`apply_snapshot_to_memory_inner` chunk-copy via raw-pointer
projection. `Kernel.memory()` accessor stays as a backwards-
compat shim that returns `Err(-EINVAL)` on the Shared variant.

### Multi-fiber story (PR #12 flip, re-applied here)
All three wasmtime Config gates flipped together in
`src/host.rs::build_engine`:
- `wasm_threads(true)` — threads proposal parser
- `shared_memory(true)` — runtime `SharedMemory::new`
- `wasm_shared_everything_threads(true)` — shared-everything-
  threads proposal (mut globals/tables across stores)

`Store` is still `!Send + !Sync` (per-Store kernel state);
cross-fiber wakeups go through shared-memory atomics on a
`SharedMemory`, not by moving a `Store` between threads.

## Out of scope (deferred follow-ups)

These are explicitly not in the bundle — they are follow-up work
that the v1 contract defers:

1. **Child-fiber resumption in fork** — v1 fork returns the
   child PID but doesn't actually start a new fiber for the
   child. Full child resumption requires `Store::call_async`
   from inside the parent (or a separate tokio task driving a
   child Store), both of which need ADR 0003 + Tier-3 thread-
   spawning infrastructure. Documented in the fork handler's
   docstring.
2. **Online drain-migration** — ADR 0003 §5 explicitly defers
   this. The freeze step must be at a quiescent point.
3. **TCP connection migration** — out of scope forever for v1
   (ADR 0003 §6).
4. **`Module::serialize` cross-host** — wasmtime's compiled
   `Module` artifact is host-arch-specific. Each host must
   re-compile its own `Module` from the `.wasm`.
5. **`SmallVec<[Waker; 2]>`** — the plan's preferred waker
   storage. v1 uses `Option<Waker>` because the typical case
   is one waiter per child and `SmallVec` is only transitive.
6. **`CLONE_VM` / `CLONE_THREAD` / `CLONE_FILES`** — rejected
   with `-EINVAL` per the clone(56) handler; landing the
   underlying shared-state machinery is behind the same
   follow-up that adds child-fiber resumption.
7. **`WUNTRACED` / `WCONTINUED` / `WNOWAIT` / `WALL`** —
   wait4 flag bits rejected with `-EINVAL`. Signal-aware wait4
   lands with a real signal delivery story.
8. **~~`edge-cli freeze` / `edge-cli serve` real bodies~~** —
   landed as P2-D3.5 (`worktree-p3-d3-5-freeze-serve` branch).
   `freeze` runs the wasm to a quiescent point (or a 10s
   timeout for server-style guests parked in `epoll_wait`) and
   writes the snap file; `serve` applies kernel-state +
   memory + inherited-listener attach in three steps; `migrate`
   is the subprocess variant (real `Command::new(current_exe)`
   freeze → serve), with `MIGRATE_IN_PROCESS=1` as a test
   opt-in. New `NR_SNAPSHOT=123` syscall + ADR 0004 wire
   contract back the freeze side. The v1 systemd-style fd
   inherit protocol is `EDGE_SERVE_FD_<N>=<parent_fd>` where
   `<N>` is the kernel fd slot matching the snapshot's
   recorded listener fd. New follow-ups listed in the D3.5
   section below.

## P2-D3.5 — `freeze` / `serve` real bodies + `migrate` subprocess

Closes the P2-D3.5 deliverable from the implementation plan
on branch `worktree-p3-d3-5-freeze-serve`.

| # | Sub-deliverable | Status |
|---|---|---|
| 1 | ADR 0004 (`docs/adr/0004-freeze-serve-wire.md`) — freeze/serve wire contract + `EDGE_SERVE_FD_<N>=<fd>` protocol | ✅ committed (`16f585c`) |
| 2 | `NR_SNAPSHOT = 123` syscall (guest-driven quiescence + env-var fallback) — 5 C tests + 2 Rust integration tests + dispatch arm + `syscall_name` registration | ✅ committed (`a7b3ad2`) |
| 3 | `edge-cli freeze <wasm> [--] [args...] --out <snap>` body (10s server-park timeout; includes the ephemeral-port-drift fix at `src/snapshot.rs:560-580`) | ✅ committed (D3.3 stub-replacement PR) |
| 4 | `Kernel::attach_inherited_listeners` plumbing (`Vec<(target_fd, source_fd)>` → `dup` + `tokio::net::TcpListener` + `Resource::Socket`) + `Socket::from_inherited_listener` + `inherited: bool` snapshot propagation | ✅ committed (`aa23ab2`) |
| 5 | `edge-cli serve <snap> <wasm> [--port <p>]` body — three-step apply (state → inherited → memory); `--port` pre-mutates snap; 11 new unit tests | ✅ committed (`32cbed7`) |
| 6 | `edge-cli migrate` subprocess rework — `Command::new(current_exe)` for freeze + serve; `MIGRATE_IN_PROCESS=1` opt-in for tests | ✅ committed (`37c5907`) |
| 7 | `tests/cli_migration_e2e.rs` (full prod-shape: freeze → snap → bind raw_fd → clear FD_CLOEXEC → spawn serve with `EDGE_SERVE_FD_<listener_fd>=<raw_fd>` → TCP probe for HTTP 200). The headline integration test. | ✅ this commit |
| 8 | HANDOFF.md regen + test totals bump | ✅ this commit |

### Test totals (P2-D3.5 close)

| Source | Count (pre D3.5 → post D3.5) |
|---|---|
| Rust unit | 123 → 144 (+21) |
| Rust integration | 197 → 210 (+13) |
| C conformance | 105 → 106 (+1, `snapshot.c`) |
| **Grand total** | **425 → 460** |

Net Rust new tests:
- `src/cli/serve.rs` parse tests: 5
- `src/cli/freeze.rs` / `cli/migrate.rs` real-body tests: 6
- `src/sys/process.rs` `snapshot` conformance: 2
- `tests/inherit_listener_conformance.rs`: 2
- `tests/cli_freeze_serve_smoke.rs` / `tests/migration_smoke.rs`: 5
- `tests/snapshot_syscall_conformance.rs`: 4
- `tests/cli_migration_e2e.rs`: 1
- Plus doc-test additions and minor unit tests in `src/snapshot.rs` /
  `src/kernel.rs`: ~6

### Architectural decisions (P2-D3.5)

#### ADR 0004 — freeze/serve wire contract
Accepted. Pins the `EDGE_SERVE_FD_<N>=<parent_fd>` env-var
shape (the `<N>` suffix is the kernel fd target slot, matching
the snapshot's recorded listener fd). The `NR_SNAPSHOT=123`
syscall is the guest-driven quiescence trigger; SIGUSR1 →
queued signal → guest syscall is deferred. Snapshot format
stays at 1 (additive `inherited: bool` field with
`#[serde(default)]` for back-compat).

### D3.5 follow-ups (deferred to a later branch)

1. **Snapshot portability check** — `serve` trusts that the
   `.wasm` path matches the freeze host's. Mismatch → apply
   succeeds but the guest mis-executes. Embed a module hash
   in `KernelSnapshot` and bump `SNAPSHOT_FORMAT_VERSION`.
2. **SIGUSR1 → `SIGSNAPSHOT` queued signal → guest syscall
   path** — host-driven quiescence for guests that don't call
   `NR_SNAPSHOT` themselves. Needs a real signal-delivery
   story; deferred until the signal-aware wait4 lands.
3. **`snapshot.c` C test portability + `chdir.c` cleanup** —
   `chdir.c` leaves a `chdir_subdir` artifact on failure
   which causes subsequent runs to fail with `EEXIST`. Add
   `MARKER_ADDR`-pinned cleanup at the start of `chdir.c`.
4. **`edge-cli bench` real body** — still a stub from D3.3.
   Land as D3.7.

## Repo hygiene post-merge

- Branch `worktree-p3-final-bundle` is ready to delete (the
  user does that as part of the merge workflow).
- Other worktree tips from earlier sessions
  (`p3-t1-futex-handler`, `p3-t3-threads-shared-memory`,
  `p2-d3-freeze-serve-and-cold-start`, etc.) — user's call
  to prune; the agent does not prune.
